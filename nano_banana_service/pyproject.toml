[project]
name = "nano-banana-service"
version = "0.1.0"
description = "Standalone Nano Banana preprocessing service"
requires-python = ">=3.12"
dependencies = [
    "google-generativeai>=0.8.0",
    "pillow",
    "python-dotenv",
]
