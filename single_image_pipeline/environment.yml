name: ns
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - pip
  - pip:
      - nerfstudio

# Usage:
#   conda env create -f single_image_pipeline/environment.yml
#   conda activate ns
# Validate:
#   python -c "import torch; print(torch.cuda.is_available())"
#   ns-train --help
